# URL of the Cloudflare Worker CORS proxy.
# For local development with `npx wrangler dev`:
VITE_WORKER_URL=http://localhost:8787

# For production (after `npx wrangler deploy`):
# VITE_WORKER_URL=https://meguru-proxy.your-subdomain.workers.dev
